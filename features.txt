handle tracks on sink not on source
	- options
		- delete if unselected
		- do not allow deselection
		- add to library
	- default: do not allow deslection
	- setting should persist
	- tracks should be red in the track list if not in the source lib
add logging support
	- remove existing log view
	- add real logging libs
	- add setting to log to file
		- default to a tmp directory
		- allow user to specify directory to save logs to
		- increment the log number if a log already exists in the dir on startup
		- show what the current log dir is
	- if possible, also add menu option to open a live log view
investigate options for getting tags for SMB shares
investigate options for getting tags for MTP devices
tracklist should be populated with the selected source even if a sink isn't selected
style the app
	- dark theme
	- light theme
	- allow user to choose or use system theme
grey out and don't allow the selection of libraries that aren't available
	- if the default libs aren't available, do no start with them selected
shift select tracks
add github action to create uberjar and attach it to the release for the matching tag when a tag like v#.#.# is made. Use that as the version number for the artifact
